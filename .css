:root {
  --bg1:  rgb(35, 37, 40);
  --card:  rgb(43, 49, 59);
  --shadow:  rgb(25, 27, 29);
  --input:  rgb(228, 239, 249);
  --inputTxt:  rgb(20, 29, 38);
  --enter:  rgb(129, 195, 245);
  --enterTxt:  rgb(7, 0, 99);
}

body {
  background-color: var(--bg1);
  color: white;
  padding: 10%;
  font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-size: 180%;
  text-align: center;
}

.div1 {
  border-radius: 18px;
  background-color: var(--card);
  height: fit-content;
  padding: 15% 5%;
  box-shadow: 5px 5px var(--shadow);
}

.maintext {
  margin: 0;
}

.subtext {
  margin-top: 5px;
}

.answer {
  display: flex;
  /* flex-direction: column; */
  margin-top: 10%;
  justify-content: center;
  align-items: center;
}

.input {
  background-color: var(--input);
  color: var(--inputTxt);
  border-radius: 6px;
  padding: 2%;
  width: 40%;
  font-size: large;
  font-weight: bolder;
}

.enter {
  background-color: var(--enter);
  color: var(--enterTxt);
  margin-left: 2%;
  padding: 2%;
  border-radius: 6px;
  width: fit-content;
  font-size: large;
  font-weight: bolder;
}